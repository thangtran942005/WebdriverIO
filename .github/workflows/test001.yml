name: Workflow name

on: [push]

jobs:
    run-commands:
        runs-on: ubuntu-latest
        steps:
            - name: Echo a string
            - run: echo "Deploy Workflow"
            - name: Echo current date
            - run: echo %date%_%time%